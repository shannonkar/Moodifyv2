import React from 'react'

function Navbar() {
    return (
        <nav className='nav'>
            <button>Login</button>
            <button>Logout</button>
        </nav>

    )
}

export default Navbar
